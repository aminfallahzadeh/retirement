import noImagePlaceholder from "@images/no-image.png";
import imagePlaceholder from "@images/image-placeholder.png";

export const placeholders = {
  noImage: noImagePlaceholder,
  image: imagePlaceholder,
};
