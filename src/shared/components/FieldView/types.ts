export type TextFieldProps = {
  label: string;
  value: string | number;
};

export type TextAreaFieldProps = {
  label: string;
  value: string;
  classNames?: string;
};

export type CheckBoxFieldProps = {
  label: string;
  checked: boolean;
};
